require 'rails_helper'

RSpec.describe User, type: :model do
  describe 'relationships' do
    # it { should have_many(:answers).inverse_of(:asker) }
    # it { should have_many(:questions).inverse_of(:answerer) }
  end
end
